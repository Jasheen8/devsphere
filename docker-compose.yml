services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: memora
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - memora_pg:/var/lib/postgresql/data
volumes:
  memora_pg:
